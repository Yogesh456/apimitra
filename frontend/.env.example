# Leave BLANK for local development (Vite proxies /api to http://localhost:5000).
# For production, set this to your deployed backend base URL (no trailing slash):
#   VITE_API_URL=https://apimitra-api.onrender.com
VITE_API_URL=
